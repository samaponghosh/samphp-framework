<?php

/**
 * SamPHP Framework — Database Bootstrap
 *
 * Loads the application configuration which contains
 * database credentials used by the Database class.
 */

require_once __DIR__ . '/config.php';